# Load broker topology (exchanges, queues, bindings, DLQs) at startup.
# Approach: pre-declare via definitions file (idempotent). Services also declare
# the same resources idempotently at their own startup — safe overlap, no conflict.
management.load_definitions = /etc/rabbitmq/definitions.json
