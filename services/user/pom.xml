<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
  <modelVersion>4.0.0</modelVersion>

  <parent>
    <groupId>org.springframework.boot</groupId>
    <artifactId>spring-boot-starter-parent</artifactId>
    <version>4.1.1</version>
    <relativePath/>
  </parent>

  <groupId>com.ecommerce</groupId>
  <artifactId>user-service</artifactId>
  <version>0.1.0</version>
  <name>user-service</name>
  <description>User Service: identity, authentication, JWT issuance, profile</description>

  <properties>
    <java.version>21</java.version>
    <!-- Pinned at 42.7.12, which carries the fix for CVE-2026-54291 (HIGH — a SCRAM-SHA-256-PLUS
         downgrade lets an attacker bypass channel-binding MITM protection). This is now one patch
         BELOW the 42.7.13 the Boot 4.1.1 BOM manages, and is held deliberately: pgjdbc is the
         mechanism behind the pod-loss readiness invariant (socketTimeout bounding a read on a dead
         socket), so moving the driver inside a version bump would destroy the attribution the
         two-commit split exists to preserve. Dropping the override so the fleet inherits 42.7.13 is
         owned by the fleet-verification slice — the override is identical in all five poms, so one
         service removing it alone would split the fleet on a shared transitive pin. -->
    <postgresql.version>42.7.12</postgresql.version>
    <jjwt.version>0.12.6</jjwt.version>
    <testcontainers.version>2.0.5</testcontainers.version>
    <spotless.version>2.43.0</spotless.version>
  </properties>

  <dependencies>
    <dependency>
      <groupId>org.springframework.boot</groupId>
      <artifactId>spring-boot-starter-web</artifactId>
    </dependency>
    <dependency>
      <groupId>org.springframework.boot</groupId>
      <artifactId>spring-boot-starter-data-jpa</artifactId>
    </dependency>
    <dependency>
      <groupId>org.springframework.boot</groupId>
      <artifactId>spring-boot-starter-validation</artifactId>
    </dependency>
    <dependency>
      <groupId>org.springframework.boot</groupId>
      <artifactId>spring-boot-starter-security</artifactId>
    </dependency>
    <dependency>
      <groupId>org.springframework.boot</groupId>
      <artifactId>spring-boot-starter-actuator</artifactId>
    </dependency>
    <dependency>
      <groupId>io.micrometer</groupId>
      <artifactId>micrometer-registry-prometheus</artifactId>
    </dependency>

    <!-- Boot 4 split the monolithic autoconfigure jar into per-technology modules, so
         FlywayAutoConfiguration now ships in spring-boot-flyway (pulled by this starter) rather
         than alongside flyway-core. Declaring only the library leaves Flyway silently NOT running:
         no migration, no warning, no error, and not one Flyway line in the whole build log. It is
         visible at all here only because ddl-auto: validate is armed — the schema user's entities
         demand would simply not exist, and startup fails loudly instead of quietly. -->
    <dependency>
      <groupId>org.springframework.boot</groupId>
      <artifactId>spring-boot-starter-flyway</artifactId>
    </dependency>
    <dependency>
      <groupId>org.flywaydb</groupId>
      <artifactId>flyway-database-postgresql</artifactId>
    </dependency>
    <dependency>
      <groupId>org.postgresql</groupId>
      <artifactId>postgresql</artifactId>
      <scope>runtime</scope>
    </dependency>

    <dependency>
      <groupId>io.jsonwebtoken</groupId>
      <artifactId>jjwt-api</artifactId>
      <version>${jjwt.version}</version>
    </dependency>
    <dependency>
      <groupId>io.jsonwebtoken</groupId>
      <artifactId>jjwt-impl</artifactId>
      <version>${jjwt.version}</version>
      <scope>runtime</scope>
    </dependency>
    <dependency>
      <groupId>io.jsonwebtoken</groupId>
      <artifactId>jjwt-jackson</artifactId>
      <version>${jjwt.version}</version>
      <scope>runtime</scope>
    </dependency>

    <!-- Test -->
    <dependency>
      <groupId>org.springframework.boot</groupId>
      <artifactId>spring-boot-starter-test</artifactId>
      <scope>test</scope>
    </dependency>
    <!-- Boot 4 split the test slices into per-technology modules: @AutoConfigureMockMvc moved to
         org.springframework.boot.webmvc.test.autoconfigure. -->
    <dependency>
      <groupId>org.springframework.boot</groupId>
      <artifactId>spring-boot-starter-webmvc-test</artifactId>
      <scope>test</scope>
    </dependency>
    <!-- Replaces spring-security-test. Boot 4 did not DROP the MockMvc slice's security
         auto-configuration, it MOVED it: spring-boot-security-test contributes its own
         AutoConfigureMockMvc.imports carrying SecurityAutoConfiguration,
         UserDetailsServiceAutoConfiguration, SecurityFilterAutoConfiguration,
         ServletWebSecurityAutoConfiguration and SecurityMockMvcAutoConfiguration. Dropping the old
         artefact because no @WithMockUser/springSecurity() call exists misses this entirely: a
         suite's security posture arrives through .imports, not through the test API, so counting
         API usages cannot see it. user declares no @WebMvcTest, so it does not hit the wall that
         forced this on product/cart/order — the swap is made anyway, because "we happen not to be
         hit today" is not a reason to leave the fleet on two different artefacts. -->
    <dependency>
      <groupId>org.springframework.boot</groupId>
      <artifactId>spring-boot-starter-security-test</artifactId>
      <scope>test</scope>
    </dependency>
    <!-- @AutoConfigureObservability is gone; @AutoConfigureMetrics lives here and is what keeps
         MetricsExposureIntegrationTest's scrape assertions running against an exporting registry
         rather than a silent no-op one. -->
    <dependency>
      <groupId>org.springframework.boot</groupId>
      <artifactId>spring-boot-micrometer-metrics-test</artifactId>
      <scope>test</scope>
    </dependency>
    <dependency>
      <groupId>org.testcontainers</groupId>
      <artifactId>testcontainers-postgresql</artifactId>
      <scope>test</scope>
    </dependency>
    <dependency>
      <groupId>org.testcontainers</groupId>
      <artifactId>testcontainers-junit-jupiter</artifactId>
      <scope>test</scope>
    </dependency>
  </dependencies>

  <dependencyManagement>
    <dependencies>
      <dependency>
        <groupId>org.testcontainers</groupId>
        <artifactId>testcontainers-bom</artifactId>
        <version>${testcontainers.version}</version>
        <type>pom</type>
        <scope>import</scope>
      </dependency>
    </dependencies>
  </dependencyManagement>

  <build>
    <plugins>
      <plugin>
        <groupId>org.springframework.boot</groupId>
        <artifactId>spring-boot-maven-plugin</artifactId>
      </plugin>
      <plugin>
        <groupId>com.diffplug.spotless</groupId>
        <artifactId>spotless-maven-plugin</artifactId>
        <version>${spotless.version}</version>
        <configuration>
          <java>
            <googleJavaFormat>
              <version>1.22.0</version>
            </googleJavaFormat>
            <removeUnusedImports/>
            <importOrder/>
          </java>
        </configuration>
        <executions>
          <execution>
            <goals>
              <goal>check</goal>
            </goals>
          </execution>
        </executions>
      </plugin>
    </plugins>
  </build>
</project>
