<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
  <modelVersion>4.0.0</modelVersion>

  <parent>
    <groupId>org.springframework.boot</groupId>
    <artifactId>spring-boot-starter-parent</artifactId>
    <version>3.5.16</version>
    <relativePath/>
  </parent>

  <groupId>com.ecommerce</groupId>
  <artifactId>payment</artifactId>
  <version>0.0.1-SNAPSHOT</version>
  <name>payment-service</name>
  <description>Payment Service — gateway charging, idempotency, outbox relay, webhook verification</description>

  <properties>
    <java.version>21</java.version>
    <!-- Override the Boot 3.5.16 BOM (42.7.11): CVE-2026-54291, HIGH — a SCRAM-SHA-256-PLUS
         downgrade lets an attacker bypass channel-binding MITM protection. Fixed in 42.7.12. -->
    <postgresql.version>42.7.12</postgresql.version>
    <jjwt.version>0.12.6</jjwt.version>
    <!-- Override the Boot 3.5.16 BOM (5.25.0) and the Boot 4.1.x BOM (5.30.0): CVE-2026-63337,
         CVE-2026-69219, CVE-2026-69220 — all HIGH, all with a fix available, so none is waivable
         under the gate policy. Advisories give `< 5.33.0` and `<= 5.33.0`, so both BOM versions are
         affected: this is pre-existing exposure, not a migration regression. The property carries
         the same name in both BOMs, so the pin survives the Boot 4 bump.
         Owner S6: delete once the destination BOM manages >= 5.33.1, and re-scan to prove it. -->
    <rabbit-amqp-client.version>5.33.1</rabbit-amqp-client.version>
    <!-- COMPANION PIN, 3.5.16-ONLY — THE SLICE THAT BUMPS THIS SERVICE DELETES THIS LINE.
         amqp-client 5.33.1 pulls netty transitively at compile scope (5.25.0 pulled none), and the
         Boot 3.5.16 BOM manages netty 4.1.135.Final = CVE-2026-59901, HIGH, fixed in 4.1.136.Final.
         Excluding netty is not an option: ConnectionFactory builds NettyConfiguration eagerly in its
         constructor, so a missing io/netty/channel/Channel is a NoClassDefFoundError at startup —
         even though spring-rabbit never selects the Netty frame handler and the classic
         SocketFrameHandlerFactory is what actually runs.
         From Boot 4.1.x the BOM manages netty 4.2.17.Final, which is already past this CVE's fix, so
         KEEPING this line after the bump would DOWNGRADE 4.2.17 -> 4.1.136 across a minor line.
         Deletion is an acceptance criterion of the bump commit, not a follow-up:
           git grep -c 'netty.version' services/<svc>/pom.xml  ->  0
           ./mvnw dependency:list | grep io.netty              ->  4.2.17.Final
         The amqp pin above STAYS after the bump — 4.1.x manages 5.30.0, still vulnerable. -->
    <netty.version>4.1.136.Final</netty.version>
    <spotless.version>2.43.0</spotless.version>
  </properties>

  <dependencies>
    <dependency>
      <groupId>org.springframework.boot</groupId>
      <artifactId>spring-boot-starter-web</artifactId>
    </dependency>
    <dependency>
      <groupId>org.springframework.boot</groupId>
      <artifactId>spring-boot-starter-data-jpa</artifactId>
    </dependency>
    <dependency>
      <groupId>org.springframework.boot</groupId>
      <artifactId>spring-boot-starter-validation</artifactId>
    </dependency>
    <dependency>
      <groupId>org.springframework.boot</groupId>
      <artifactId>spring-boot-starter-security</artifactId>
    </dependency>
    <dependency>
      <groupId>org.springframework.boot</groupId>
      <artifactId>spring-boot-starter-actuator</artifactId>
    </dependency>
    <!-- Prometheus scrape format for /actuator/prometheus (version managed by Boot BOM). -->
    <dependency>
      <groupId>io.micrometer</groupId>
      <artifactId>micrometer-registry-prometheus</artifactId>
    </dependency>
    <dependency>
      <groupId>org.springframework.boot</groupId>
      <artifactId>spring-boot-starter-amqp</artifactId>
    </dependency>

    <dependency>
      <groupId>org.flywaydb</groupId>
      <artifactId>flyway-core</artifactId>
    </dependency>
    <dependency>
      <groupId>org.flywaydb</groupId>
      <artifactId>flyway-database-postgresql</artifactId>
    </dependency>
    <dependency>
      <groupId>org.postgresql</groupId>
      <artifactId>postgresql</artifactId>
      <scope>runtime</scope>
    </dependency>

    <!-- JWT validation (no issuance in Payment) -->
    <dependency>
      <groupId>io.jsonwebtoken</groupId>
      <artifactId>jjwt-api</artifactId>
      <version>${jjwt.version}</version>
    </dependency>
    <dependency>
      <groupId>io.jsonwebtoken</groupId>
      <artifactId>jjwt-impl</artifactId>
      <version>${jjwt.version}</version>
      <scope>runtime</scope>
    </dependency>
    <dependency>
      <groupId>io.jsonwebtoken</groupId>
      <artifactId>jjwt-jackson</artifactId>
      <version>${jjwt.version}</version>
      <scope>runtime</scope>
    </dependency>

    <!-- commons-codec for hex encoding (HMAC-SHA256 webhook verification) -->
    <dependency>
      <groupId>commons-codec</groupId>
      <artifactId>commons-codec</artifactId>
    </dependency>

    <!-- Test -->
    <dependency>
      <groupId>org.springframework.boot</groupId>
      <artifactId>spring-boot-starter-test</artifactId>
      <scope>test</scope>
    </dependency>
    <dependency>
      <groupId>org.springframework.security</groupId>
      <artifactId>spring-security-test</artifactId>
      <scope>test</scope>
    </dependency>
    <dependency>
      <groupId>org.springframework.amqp</groupId>
      <artifactId>spring-rabbit-test</artifactId>
      <scope>test</scope>
    </dependency>
    <dependency>
      <groupId>org.testcontainers</groupId>
      <artifactId>junit-jupiter</artifactId>
      <scope>test</scope>
    </dependency>
    <dependency>
      <groupId>org.testcontainers</groupId>
      <artifactId>postgresql</artifactId>
      <scope>test</scope>
    </dependency>
    <dependency>
      <groupId>org.testcontainers</groupId>
      <artifactId>rabbitmq</artifactId>
      <scope>test</scope>
    </dependency>
    <!-- Real HTTP stand-in for the Order seam (OrderClient over a socket, never mocked) -->
    <dependency>
      <groupId>org.wiremock</groupId>
      <artifactId>wiremock-standalone</artifactId>
      <version>3.9.2</version>
      <scope>test</scope>
    </dependency>
  </dependencies>

  <dependencyManagement>
    <dependencies>
      <dependency>
        <groupId>org.testcontainers</groupId>
        <artifactId>testcontainers-bom</artifactId>
        <version>1.21.4</version>
        <type>pom</type>
        <scope>import</scope>
      </dependency>
    </dependencies>
  </dependencyManagement>

  <build>
    <plugins>
      <plugin>
        <groupId>org.springframework.boot</groupId>
        <artifactId>spring-boot-maven-plugin</artifactId>
      </plugin>

      <plugin>
        <groupId>com.diffplug.spotless</groupId>
        <artifactId>spotless-maven-plugin</artifactId>
        <version>${spotless.version}</version>
        <configuration>
          <java>
            <googleJavaFormat/>
            <removeUnusedImports/>
          </java>
        </configuration>
        <executions>
          <execution>
            <goals>
              <goal>check</goal>
            </goals>
          </execution>
        </executions>
      </plugin>
    </plugins>
  </build>
</project>
